{
  "name": "@gestion/database",
  "version": "0.1.0",
  "private": true,
  "main": "src/index.ts",
  "scripts": {
    "generate": "prisma generate",
    "migrate:dev": "prisma migrate dev",
    "migrate:deploy": "prisma migrate deploy",
    "seed": "node prisma/seed.js",
    "studio": "prisma studio",
    "build": "echo \"Database package does not require build\"",
    "lint": "echo \"No lint configured for @gestion/database\"",
    "test": "echo \"No tests yet for @gestion/database\""
  },
  "dependencies": {
    "@prisma/client": "^5.22.0",
    "dotenv": "^16.4.5"
  },
  "devDependencies": {
    "prisma": "^5.22.0",
    "typescript": "^5.8.3"
  },
  "prisma": {
    "seed": "node prisma/seed.js"
  }
}
